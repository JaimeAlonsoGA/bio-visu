import { View, Text } from 'react-native'
import React from 'react'

//display all images from a SECTION. Section.map where source = image, class = name. 
const Gallery = () => {
  return (
    <View>
      <Text>gallery</Text>
    </View>
  )
}

export default Gallery