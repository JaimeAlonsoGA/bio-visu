import { Dimensions } from "react-native";

export const { width, height } = Dimensions.get("window");

export const mainViewStyle = {backgroundColor: "#d9e1ff", height: height, width: width};

export const containerColor = [
    { color: "#ff7d54" },
    { color: "#0de149" },
    { color: "#7ee0ff" },
  ];